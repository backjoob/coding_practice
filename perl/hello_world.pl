#!/usr/bin/perl

# these are recommended because perl allows confusing things by default
use strict;
use warnings;

print "Hello, world!\n";

# This is the same because of the newline character inside the quotes:
#print "Hello, world!
#";
